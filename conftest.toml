policy = "tests/policy"
namespace = "tests.policy"
no_color = true
output = "json"
# SOPS-encrypted environment files contain JSON, not dotenv input.
ignore = '.git/|.devcontainer/|.vscode/|build/|vendor/|logs/|CODEOWNERS|LICENSE|(^|/)\.env(\.[^/]+)?\.enc$'
